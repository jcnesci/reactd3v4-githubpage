// src/components/Histogram/index.js
import Histogram from './Histogram';

export default Histogram;

//?:  Swizec says we could've used a 're-export' (ie. "export { default } from './Histogram';") like in CountyMap/index.js, instead of the 2 lines above. What exactly is a re-export?